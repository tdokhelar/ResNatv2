{% extends '@SonataAdmin/CRUD/base_list.html.twig' %}
{% trans_default_domain 'admin' %}

{% block actions %}
  {{parent()}}
  <li><a class="sonata-action-element" href="{{ path('gogo_authorized_projects_changes_history') }}"><i class="fa fa-history" aria-hidden="true"></i>
    <span>{{ 'authorized_projects.buttons.changesHistory'|trans }}</span>
  </a></li>
{% endblock %}

{% block list_table %}
  <div class="col-md-12">
    <div class="well">
      <div style="display:flex; align-items:center">
        <i class="fa fa-lightbulb-o" style="margin-right:16px;font-size:24px"></i>
        <div>{{ 'authorized_projects.help'|trans }}</div>
        </div>
      </div>
    </div>
    {{parent()}}
  </div>
{% endblock %}